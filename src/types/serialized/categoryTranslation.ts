import { CategoryTranslationDTO } from '../dtos/categoryTranslation';

// For now, SerializedCategoryTranslation can be the same as CategoryTranslationDTO
// Adjust if specific serialization differences are needed
export type SerializedCategoryTranslation = CategoryTranslationDTO;
